import styled from "@emotion/styled";

const NavWrapper = styled.nav`

`;

const Navigation = () => {
  return (
    <NavWrapper>
        test nav
    </NavWrapper>
  );
}

export default Navigation;
